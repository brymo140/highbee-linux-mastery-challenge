# Day 1 Practice Drill

## Task
I print my working directory, then navigate to /var/log using an absolute 
path, back to my home directory using ~, and then back to /var/log 
using cd -. I listed its contents in long, human-readable format as asked.

## Commands I ran
pwd
cd /var/log
cd ~
cd -
ls -lh
