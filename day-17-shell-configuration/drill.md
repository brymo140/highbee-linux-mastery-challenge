# Day 17 Practice Drill

## Task
I added a permanent environment variable and a custom alias to my '.bashrc' using "nano ~/.bashrc". I then reloaded it without opening a new terminal using "source ~/.bashrc" and then confirmed if both persist in a new terminal window.

## Commands I ran
- nano ~/.bashrc
- export MY_FAVOURITE_CAR="I must have many exortic cars"
- alias lol="ls -la"
- source ~/.bashrc
- echo $MY_FAVOURITE_CAR
- lol
