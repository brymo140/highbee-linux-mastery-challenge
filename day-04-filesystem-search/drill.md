# Day 4 Practice Drill

## Task
I ran 'find /etc -name "*.conf"' to find every .conf file under the /etc directory.  After that, i ran 'find /var -size +1M' to find every file larger than 1mb in var directory. After that, i also ran 'du -sh /home' to be able to total report of the disk usage of the home directory which result is 15MB and then i ran 'df -h /' to know the remaining space on the root filesystem which also gives the result that the remaining free space is 955G.

## Commands I ran
find /etc -name "*.conf"
find /var -size +1M
du -sh /home
df -h /
