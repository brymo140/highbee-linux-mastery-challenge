chown: This command which i translate as change ownership is used to change the owner of a file or folder to a different user account on the system. You may always need administrative priviledge like (sudo) to run it.
chown user:group:This command chnages the owner and the group of an item at the exact same time. One Example is running (sudo chown john: developers report.txt) ehich set the name of the owner to the name of thr owner and the goup to the developers team.
chown -R: this command using "-R" will recursively change the owner and the group for a folder and everything inside it, including all subfolders and files. Example is running (chown -R Highbee:developer /log) which will change the user and the group of /log folder and everything inside it to highbee and developer repectively.
chgrp: This is used to cange the group of a file or a folder without touching the user owner.
chmod u+s (SUID):This is used to change the modification of a file or folder ut instead of changing the permission, it tampers with the user privileges. As the (SUID) means set user identity, it applies to an executable file so that any user running that file executes it with the privileges o the file's owner, rather than their own. So, this changes the execute i the user's bit to s (e.g., -rwsr-xr-x). This is how the standard passwd command lets normal users modify the secure /etc/shaodw file where user passowrd are kept.
chmod g+s (SGID): This used to do the same thing (SUID) does but here is towards the group part which means Set Group ID.  
chmod +t (sticky bit):This is used to protect files from accidental deletion on shared directories like "/temp". When active, a user can only delete or rename files they personally own, even if other users have full write permission to the folder. The others x changes to 't' (e.g., drwxrwxrwt).
find -perm /4000: It searches your system for filrs that have the SUID bit set which "4" is used to represent in the four digit octal form). 
getfacl: This command displays the access control list in details for an item. It will show exactly which individual users and groups have been granted custom access rights.
setfacl -m: This one modifies the ACl (access control list) of a file. The tier (u, g), the name, and the permissions will be specified. An exaple is (setfacl -m u:mary:rw file.sh)
