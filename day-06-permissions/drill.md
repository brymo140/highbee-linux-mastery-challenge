# Day 6 Practice Drill

## Task
I created three script files and set them all from their default permission to 'rwxr-xr-x' using all three chmod methods and confirmed each files with ls -l after each change. 

## Commands I ran
ls -l
chmod (relative +/-)
chmod (assignment =)
chmod 755(octal)
